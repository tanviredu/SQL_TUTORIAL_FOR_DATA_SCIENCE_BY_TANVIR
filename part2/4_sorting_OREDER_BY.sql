

-- when you query data it will return the data but with no order like ramdom order

-- you can oreder the data with the ORDER BY clause

-- sorting data is very important because 

-- it gives the insight of the data

-- syntax

SELECT something 
FROM database
ORDER BY charistics



-- rules 

--1) you can order single column
--2) you can use multiple column
--3) you can sort by thnig evern you donr select it
--4) you have to add the ORDER BY clause at the last of the statement

